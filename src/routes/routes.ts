import type { TRoute } from '@/types/routes/types'

import { default as routesConstants } from '@/constants/routes/routesConstants.json'

export const routes: TRoute[] = routesConstants as unknown as TRoute[]

'use client'

import React from 'react'

import { IntroductionCardCanvasInterface } from '@/interfaces/components/r3f/introductionCardInterfaces'

export default function IntroductionCardCanvas({ content }: IntroductionCardCanvasInterface) {
  return (
    <div>index</div>
  )
}
